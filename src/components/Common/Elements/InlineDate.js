import React from 'react';
import Checkbox from '../Checkbox';

const InlineDate = props => <Checkbox {...props} label="Inline" tooltip="Display the date picker inline with the input. Do not require to click the input field to trigger the date picker." />
export default InlineDate;