import React from 'react';
import Textarea from '../Textarea';

const After = props => <Textarea {...props} label="After" description="Custom HTML displayed after field output" />
export default After;