import React from 'react';
import Checkbox from '../Checkbox';

const ForceDelete = props => <Checkbox {...props} label="Force delete" tooltip="Delete files from the Media Library when deleting them from post meta." />
export default ForceDelete;