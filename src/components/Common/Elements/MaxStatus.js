import React from 'react';
import Checkbox from '../Checkbox';

const MaxStatus = props => <Checkbox {...props} label="Show status" tooltip="Display how many files uploaded/remaining" />
export default MaxStatus;