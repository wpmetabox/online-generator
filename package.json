{
  "name": "online-generator",
  "homepage": "https://metabox.io/online-generator/",
  "version": "2.0.0",
  "license": "GPL-2.0-or-later",
  "dependencies": {
    "dot-prop": "^6.0.1",
    "react": "^17.0.1",
    "react-clipboard.js": "^2.0.16",
    "react-dom": "^17.0.1",
    "react-highlight": "^0.12.0",
    "react-scripts": "4.0.1",
    "react-tabs": "^3.1.1",
    "react-tooltip": "^4.2.7",
    "slugify": "^1.4.6"
  },
  "scripts": {
    "css": "node-sass --output-style compressed -x sass/style.scss -o public",
    "postcss": "postcss public/style.css -u autoprefixer --no-map -o public/style.css --autoprefixer.browsers=\"last 2 versions\"",
    "watch": "node-sass -w sass -o public",
    "deploy": "scp -r build/* metabox:/home/bitnami/apps/wordpress/htdocs/online-generator/",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "autoprefixer": "^10.1.0",
    "node-sass": "^5.0.0",
    "postcss": "^8.2.2",
    "postcss-cli": "^8.3.1"
  }
}
