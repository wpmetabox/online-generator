{
  "name": "online-generator",
  "homepage": "https://metabox.io/online-generator/",
  "version": "2.0.0",
  "license": "GPL-2.0-or-later",
  "dependencies": {
    "dot-prop": "^6.0.1",
    "react": "^18",
    "react-clipboard.js": "^2",
    "react-dom": "^18",
    "react-highlight": "^0.15",
    "react-scripts": "^5",
    "react-tabs": "^6",
    "react-tooltip": "^5",
    "slugify": "^1"
  },
  "scripts": {
    "css": "sass --no-source-map -s compressed sass/style.scss public/style.css",
    "watch": "sass --no-source-map -s compressed -w sass/style.scss public/style.css",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
